{
  "name": "analizador-vulnerabilidades-web",
  "version": "1.0.0",
  "description": "Analizador de seguridad web: cabeceras, vectores XSS y heurística de ofuscación, con controles anti-SSRF, timeout y límite de tamaño. Sin dependencias.",
  "main": "index.js",
  "bin": {
    "analizador-vuln": "index.js"
  },
  "scripts": {
    "start": "node index.js",
    "test": "node --test",
    "lint": "eslint .",
    "check": "node --check index.js && npm run lint && npm test"
  },
  "devDependencies": {
    "@eslint/js": "^9.0.0",
    "eslint": "^9.0.0"
  },
  "license": "MIT"
}
